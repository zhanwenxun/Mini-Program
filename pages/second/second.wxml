<!--pages/second/second.wxml-->
<view class="container">

  <button class="return" bindtap='ToWork'>返回</button>

  <view class="second" >  
    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==0}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url1}}' data-src='{{url1}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>

    <view class="workline"></view>

    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==1}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url2}}' data-src='{{url2}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class='bulk'></view>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>

    <view class="workline"></view>

    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==2}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url3}}' data-src='{{url3}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>

    <view class="workline"></view>

    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==3}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url4}}' data-src='{{url4}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class='bulk'></view>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>

    <view class="workline"></view>

    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==4}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url5}}' data-src='{{url5}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class='bulk'></view>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>

    <view class="workline"></view>

    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==5}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url6}}' data-src='{{url6}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>

    <view class="workline"></view>

    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==6}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url7}}' data-src='{{url7}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class='bulk'></view>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>

    <view class="workline"></view>

    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==7}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url8}}' data-src='{{url8}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class='bulk'></view>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>

    <view class="workline"></view>

    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==8}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url9}}' data-src='{{url9}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>

    <view class="workline"></view>

    <block wx:for="{{listData}}" wx:key="{[name]}" wx:if="{{index==9}}">
      <view class="name">
        <text class="workname">
          <text class="gray">●</text>  {{item.name}}<text class="gray">●</text>
        </text>
      </view>
      <view class="imagearea">
        <image class="image" style="display:flex; " bindtap="previewImage" mode="widthFix" src='{{url10}}' data-src='{{url10}}'></image>
      </view>
      <text style="font-size: 14px; display: flex; justify-content: center;text-align: center">（长按预览图片可识别二维码）</text>
      <view class="textarea">
        <text class="left">简介\n</text>
        <view class="smallspace"></view>
        <p class="introduce"><text class="para"> {{item.introduce}} </text></p>
        <view class='bulk'></view>
        <view class="line"></view>
        <view class="school">
          <image class="icon" src="../images/school.png"></image>
          <text class="text"> {{item.school }} </text>
        </view>
        <view class="band">
          <image class="icon" src="../images/band.png"></image>
          <text class="text"> {{item.band}} </text>
        </view>
        <view class="member">
          <image class="icon" src="../images/member.png"></image>
          <text class="text"> {{item.member}} </text>
        </view>
      </view>
    </block>
  </view>
</view>